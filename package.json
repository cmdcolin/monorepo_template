{
  "name": "monorepo_test",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Colin",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "test": "pnpm --parallel -r test",
    "clean": "pnpm -r exec -- rm -rf node_modules dist"
  }
}
